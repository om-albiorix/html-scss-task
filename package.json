{
  "scripts": {
    "watch-sass": "node-sass -w css/style.scss css/style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-sass": "^9.0.0",
    "nodemon": "^2.0.22"
  }
}
